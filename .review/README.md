# Temporary review artifact — do not merge

This directory exists so that a proposal to `~/dotfiles/agent-instructions/shared.md` can be
reviewed with inline comments on a pull request. It is not part of MIPVerify.

Delete this directory and close the pull request once the proposal is settled. Nothing here belongs
on `master`.
